# Mesh Router Stack Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# PROVIDER CONFIGURATION
# =============================================================================
# Connection string format: <backend_url>,<userid>,<signature>
# Get these values from your NSL.sh or Yundera account
PROVIDER=https://nsl.sh,your_user_id,your_signature

# Backend URL for mesh-router services
BACKEND_URL=https://nsl.sh

# =============================================================================
# PRIVATE KEY (for signature regeneration)
# =============================================================================
# Your Ed25519 private key - keep this safe!
# Used to regenerate the signature if needed (e.g., key rotation, new provider).
# The signature in PROVIDER is derived from this key.
# Not used at runtime, but recommended to keep for future reference.
PRIVATE_KEY=your_private_key_here

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================
# Your public IP address (IPv4 or IPv6)
# Leave empty to auto-detect (mesh-router-agent only)
PUBLIC_IP=

# Domain assigned to your PCS instance
PCS_DOMAIN=yourname.nsl.sh

# =============================================================================
# CASAOS CONFIGURATION
# =============================================================================
# Default password for CasaOS admin
PCS_DEFAULT_PASSWORD=changeme

# Email for SSL certificates and notifications
PCS_EMAIL=admin@yourname.nsl.sh

# Data root directory (where app data is stored)
PCS_DATA_ROOT=/DATA

# Host path for data volume (adjust for your system)
# Linux: /home/user/DATA
# Windows: /c/DATA
# Mac: /Users/user/DATA
DATA_HOST_PATH=/c/DATA
